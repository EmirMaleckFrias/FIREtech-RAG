# Python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node
frontend/node_modules/
frontend/dist/

# Secretos (el .env.example SÍ se versiona)
backend/.env
.env
.env*
!backend/.env.example
!.env.example
backend/.supabase_db_password.txt
backend/.app_access_key.txt

# Datos del negocio: catálogos con costos internos confidenciales.
# No se versionan; la ingesta los espera en data/raw (PDF) y data/raw_xlsx (Excel).
data/

# Resultados de evals (results.json + report.md por corrida); los docs/EVAL_*.md sí se versionan.
backend/evals/results/

# Vercel
.vercel

# SO / IDE
.DS_Store
Thumbs.db
.vscode/
# Reportes de evaluación pueden contener preguntas o respuestas sensibles.
backend/evals/results/
frontend/evals/results/

# node_modules en cualquier nivel (la raiz tambien: vitest deja resultados ahi)
node_modules/

# Casos de evaluación del RAG: preguntas y respuestas esperadas sobre documentos
# clínicos de cada cuenta. Solo se versiona la plantilla; los reportes ya
# estaban ignorados más arriba (frontend/evals/results/).
frontend/evals/*.jsonl
!frontend/evals/alzheimer.template.jsonl
